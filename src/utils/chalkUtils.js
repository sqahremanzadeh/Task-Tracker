import chalk from "chalk";

const errorOut = chalk.bold.red;
const warning = chalk.bold.yellow;
const success = chalk.bold.green;
const tableOutput = chalk.reset;

export { errorOut, warning, success, tableOutput };
